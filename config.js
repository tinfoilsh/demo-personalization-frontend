// Default control-server URL. Override at runtime on the connect screen
// (persisted to localStorage under `pz.apiBase`).
window.DEFAULT_API_BASE =
  "https://demo-personalization.debug.tinfoil.containers.tinfoil.dev";
